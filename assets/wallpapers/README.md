# Wallpapers

Put your wallpapers here with these names:

`wallpaper.jpg` (static, used by `hyprpaper`)

`wallpaper.mp4` (optional, for video wallpaper setups)

After `nixos-rebuild`, Home Manager links this folder to:

`~/.config/wallpapers/`

Hyprpaper reads files from:

`~/.config/wallpapers/`
